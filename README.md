# code-canvas

Visual Token-Budget Context Packager for LLMs.

## Objective
Make it Token-Budget-Centric. Let the user drag a slider saying "I have a 16k token limit." The tool then automatically ranks files by import dependencies, git recency, or AST analysis and pre-selects the most critical files that fit perfectly within that budget, auto-summarizing or collapsing the rest.
